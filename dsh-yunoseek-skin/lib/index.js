//#region src/index.ts
/**
* Node half of the dsh-yunoseek-skin bundle. This package contributes browser
* presentation only — the token layer, the art-direction sheet, and the brand
* occupants all live in the ./client half — so the node row is an empty body.
*/
function apply() {}
//#endregion
export { apply };
